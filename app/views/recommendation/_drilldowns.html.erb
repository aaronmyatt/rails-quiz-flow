<ol>
  <% drill_list = Drilldown.find_by_sql ["SELECT name FROM drilldowns WHERE user_id = ?", User.last.id] %>
  <% drill_list.each do |x|  %>
    <li>
        <%=  x.name %>
    </li>
    <% end %>
</ol>