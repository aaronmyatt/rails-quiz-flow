<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>The Questions Index</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.7.2/css/bulma.min.css">
    <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
  </head>
  <body>
    <section class="section">
        <div class="container">
      <h1 class="title">

       Please Answer The Following Questions

      </h1>
      <nav class="breadcrumb has-succeeds-separator" aria-label="breadcrumbs">
        <ul>
          <li><a href="/category/index" >Category</a></li>
          <li ><a href="/drilldown/index" >Drilldown</a></li>
          <li class="is-active"><a href="/questions/index" aria-current="page">Questions</a></li>
          <li><a href="/reccomendation/index">Reccomendation</a></li>
        </ul>
      </nav>
      </div>
    </section>

    <div class="container">
<%= form_tag( '/questions/form' , remote: true, method: 'post') do |f| %>
  <div class = "columns">
    <div class = "column">
      <ul>
        <% @questions1.each do |q| %>
          <li>
            <%= label_tag "#{q.drilldown_name}:#{q.question}" %>
            <%= select_tag "questions[#{q.question}]", options_for_select(1..10, 5) %>
            <br>
            <br>
          </li>
        <% end %>
      </ul>
    </div>
    <div class = "column">
      <ul>
        <% @questions2.each do |q| %>
          <li>
          <%= label_tag "#{q.drilldown_name}:#{q.question}" %>
          <%= select_tag "questions[#{q.question}]", options_for_select(1..10, 5) %>
          <br>
          <br>
          </li>
        <% end %>
      </ul>
    </div>
    <div class = "column">
      <ul>    
        <% @questions3.each do |q| %>
          <li>        
            <%= label_tag "#{q.drilldown_name}:#{q.question}" %>
            <%= select_tag "questions[#{q.question}]", options_for_select(1..10, 5) %>
            <br>
            <br>
          </li>
        <% end %>
      </ul>
    </div>
    <div class = "column">
      <ul>
        <% @questions4.each do |q| %>
        <li>
          <%= label_tag "#{q.drilldown_name}:#{q.question}" %>
          <%= select_tag "questions[#{q.question}]", options_for_select(1..10, 5) %>
          <br>
          <br>
        </li>
          <% end %>
      </ul>
    </div>
</div>
   <button class="button is-link">
        Save
    </button>
<% end %>

</div>
  </body>
</html>
